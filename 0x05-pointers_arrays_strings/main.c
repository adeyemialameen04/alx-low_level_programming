#include <stdio.h>
#include "main.h"

int main(void)
{
    // int n = 900;
    // printf("%d\n", n);
    //  reset_to_98(&n);
    // printf("%d\n", n);
    // int x = 0, y=0;
char *str;
str = "Al-ameen";
int len = 0;
len = _strlen(str);
printf("%d",len);
    return(0);
}