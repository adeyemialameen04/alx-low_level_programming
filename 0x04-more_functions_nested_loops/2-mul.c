#include "main.h"

/**
 * mul - Multiply two numbers.
 *
 * @a: First operand.
 * @b: Second operand.
 *
 * Return: The multiplied number (result).
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
