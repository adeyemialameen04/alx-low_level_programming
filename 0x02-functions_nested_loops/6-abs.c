#include "main.h"
#include <stdio.h>

/**
 * _abs - Funtion to compute the absolute value of an integer.
 *
 * @x: The number to be checked.
 *
 * Return: 1 if the number is a negative, and 0 if its positive.
 */
int _abs(int x)
{
if (x < 0)
{
return (1);
}
else
{
return (0);
}
}
